__pycache__/
matching/engine/target/
*.py[cod]
.pytest_cache/
.venv/
.DS_Store
# Core dumps only; core/ is the JavaScript library.
core.[0-9]*
# Local agent worktrees and settings.
.claude/
# Agent instruction files never ship in the plugin tree (marketplace review); keep any local ones untracked.
CLAUDE.md
AGENTS.md
